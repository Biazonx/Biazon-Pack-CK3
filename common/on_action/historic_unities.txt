on_game_start = {
	effect = {
		if = {
			limit = { game_start_date = 867.1.1 }
			# Twiddle some starting unities.
			## The Abassids are in the middle of a self-killing frenzy, so we lower theirs substantially.
			house:house_abbasid ?= {
				add_unity_value = {
					value = -100
					# This is from historical circumstances, so we just do use the house head.
					character = house_head
					desc = clan_unity_historical_circumstances.desc
				}
			}
			## The Samanids are juuuuust about to get started on killing each other over who gets to lead Transoxiana.
			house:house_samanid ?= {
				add_unity_value = {
					value = -40
					# This is from historical circumstances, so we just do use the house head.
					character = house_head
					desc = clan_unity_historical_circumstances.desc
				}
			}
			## The Afrighids (both of them) are having fairly few arguments because only one of them can speak and it's very easy to manage relations with a baby.
			dynasty:1042112.dynast.house ?= {
				add_unity_value = {
					value = 50
					# This is from historical circumstances, so we just do use the house head.
					character = house_head
					desc = clan_unity_historical_circumstances.desc
				}
			}
			## The Tahirids are scattered but actually get along quite well and support each other politically (mostly).
			dynasty:811.dynast.house ?= {
				add_unity_value = {
					value = 100
					# This is from historical circumstances, so we just do use the house head.
					character = house_head
					desc = clan_unity_historical_circumstances.desc
				}
			}
			## The Umayyads are having something of a renaissance.
			dynasty:597.dynast.house ?= {
				add_unity_value = {
					value = 100
					# This is from historical circumstances, so we just do use the house head.
					character = house_head
					desc = clan_unity_historical_circumstances.desc
				}
			}
		}
		if = {
			limit = { game_start_date = 1066.9.15 }
				## Seljuks: After the death of Alp Arslan, his heir, Malik-shah, and brother, Qavurt, fought over the empire then fell into chaos after maliks shahs death
			house:house_seljuk ?= {
				add_unity_value = {
					value = -100
					# This is from historical circumstances, so we just do use the house head.
					character = house_head
					desc = clan_unity_historical_circumstances.desc
			}
		}
			## Ghazvanids: the Ghazvids where in their golden age at 1066 and only saw dynastic strife at the start of the 12th century
			dynasty:790.dynast.house ?= {
				add_unity_value = {
					value = 100
					# This is from historical circumstances, so we just do use the house head.
					character = house_head
					desc = clan_unity_historical_circumstances.desc
				}
			}
			## Almoravid: these guys did have internal disagreements and rivalries but this never escalated to a full blown civil war in this period
			house:house_almoravid ?= {
				add_unity_value = {
					value = 50
					# This is from historical circumstances, so we just do use the house head.
					character = house_head
					desc = clan_unity_historical_circumstances.desc
				}
		}
	}
		## 1178.
		if = {
			limit = { game_start_date = 1178.10.1 }
			## Abbasids: they where resurgent in the 12th century, shame about the mongols.
			house:house_abbasid ?= {
				add_unity_value = {
					value = 50
					# This is from historical circumstances, so we just do use the house head.
					character = house_head
					desc = clan_unity_historical_circumstances.desc
				}
			}
			## Ayyubids: saladins sons tried to reunited their fathers conquests over each others bodies
			dynasty:24000.dynast.house ?= {
				add_unity_value = {
					value = -100
					# This is from historical circumstances, so we just do use the house head.
					character = house_head
					desc = clan_unity_historical_circumstances.desc
				}
			}
			## Khwarezmids: Tekish's rule was contested by his brother and thats about it but ill make them competitive to encourage conquest
			dynasty:101866.dynast.house ?= {
				add_unity_value = {
					value = -50
					# This is from historical circumstances, so we just do use the house head.
					character = house_head
					desc = clan_unity_historical_circumstances.desc
				}
			}
			## Seljuks: the sons of Kilij Arslan II where already fighting each other while he ruled and only got worse when he died while the seljuks in persia where fighting to be relevent in their own land
			house:house_seljuk ?= {
				add_unity_value = {
					value = -50
					# This is from historical circumstances, so we just do use the house head.
					character = house_head
					desc = clan_unity_historical_circumstances.desc
				}
			}
			## Mu'minids: bit of a dull period for the almohads in most regards, this family remained closely united until bureacrats tried to coup the caliphate in 1224.
			dynasty:101875.dynast.house ?= {
				add_unity_value = {
					value = 100
					# This is from historical circumstances, so we just do use the house head.
					character = house_head
					desc = clan_unity_historical_circumstances.desc
				}
			}
			## Ghurids: the Ghurids brothers ruled together but their heirs didnt follow suit
			dynasty:791.dynast.house ?= {
				add_unity_value = {
					value = 50
					# This is from historical circumstances, so we just do use the house head.
					character = house_head
					desc = clan_unity_historical_circumstances.desc
				}
			}
		}
	}
}