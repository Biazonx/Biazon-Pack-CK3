#Creating Kingdom of Romania
form_romania_kingdom_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_found_kingdom.dds"
	}

	desc = form_romania_kingdom_decision_desc
	selection_tooltip = form_romania_kingdom_decision_tooltip
	decision_group_type = regional_major

	ai_check_interval_by_tier = {
		barony = 0
		county = 120
		duchy = 120
		kingdom = 120
		empire = 0
		hegemony = 0
	}

	is_shown = {
		NOT = { exists = title:k_romania.holder }
		NOT = {  # Can only do it once.
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_formed_romania_kingdom
			}
		}
		# No kings or emperors...
		highest_held_title_tier <= tier_kingdom
		any_held_title = {
			tier = tier_county
			title_province = { geographical_region = custom_eastern_balkans }
		}
	}

	is_valid = {
		is_independent_ruler = yes
		# Must be suitably prestigious.
		prestige_level >= 2
		# Must control core Romania
		completely_controls_region = custom_romania
	}

	cost = {
		gold = 250
	}

	effect = { 
		form_romania_scripted_effect = yes
		add_prestige = 500
		dynasty = { add_dynasty_prestige = 250 }
		
		house = {
			add_house_modifier = {
			modifier = romania_title_reward_house_modifier 
				years = 200
			}
		}
	}
	
	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 50
	}
}

form_yugoslavia_empire_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_found_kingdom.dds"
	}
	desc = form_yugoslavia_empire_decision_desc
	selection_tooltip = form_yugoslavia_empire_decision_tooltip
	decision_group_type = regional_major
	ai_check_interval = 120
	
	is_shown = {
		NOT = { exists = title:e_yugoslavia.holder }
		NOT = {  # Can only do it once.
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_formed_yugoslavia_empire
			}
		}
		# No kings or emperors...
		highest_held_title_tier <= tier_kingdom
		any_held_title = {
			tier = tier_county
			title_province = { geographical_region = custom_yugoslavia }
		}
	}

	is_valid = {
		is_independent_ruler = yes
		# Must be suitably prestigious.
		prestige_level >= 2
		# Must control core Yugoslavia.
		completely_controls_region = custom_yugoslavia
	}

	effect = { 
		form_yugoslavia_scripted_effect = yes
		add_prestige = 1000
		dynasty = { add_dynasty_prestige = 500 }
	}
	
	cost = {
		gold = 250
	}
	
	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100
	}
}

form_czechslovakia_empire_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_found_kingdom.dds"
	}
	desc = form_czechslovakia_empire_decision_desc
	selection_tooltip = form_czechslovakia_empire_decision_tooltip
	decision_group_type = regional_major
	ai_check_interval = 120
	
	is_shown = {
		NOT = { exists = title:e_czechslovakia.holder }
		NOT = {  # Can only do it once.
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_formed_czechslovakia_empire
			}
		}
		# No kings or emperors...
		highest_held_title_tier <= tier_kingdom
		any_held_title = {
			tier = tier_county
			title_province = { geographical_region = custom_czechslovakia }
		}
	}

	is_valid = {
		is_independent_ruler = yes
		# Must be suitably prestigious.
		prestige_level >= 2
		# Must control core Yugoslavia.
		completely_controls_region = custom_czechslovakia
	}

	effect = { 
		form_czechslovakia_scripted_effect = yes
		add_prestige = 1000
		dynasty = { add_dynasty_prestige = 500 }
	}
	
	cost = {
		gold = 250
	}
	
	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100
	}
}

bzn_embrace_german_culture_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_culture.dds"
	}

	decision_group_type = regional_major

	ai_check_interval = 120

	is_shown = {
		NOT = { exists = title:k_teutonic.holder }
		NOT = {  # Can only do it once.
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_formed_teutonic_kingdom
			}
		}
		faith = faith:catholic
		OR = {
			culture = { has_cultural_pillar = heritage_north_germanic }
			culture = { has_cultural_pillar = heritage_central_germanic }
		}
	}

	is_valid = {
		OR = {
			culture = { has_cultural_pillar = heritage_north_germanic }
			culture = { has_cultural_pillar = heritage_central_germanic }
		}
		piety_level >= 2
		completely_controls_region = custom_teutonic
		highest_held_title_tier >= tier_duchy
	}

	desc = embrace_german_culture_decision_desc
	cost = { gold = 500 piety = 500 }

	effect = {
		form_teutonic_scripted_effect = yes	
		give_nickname = nick_teut
		add_prestige = 500
		dynasty = { add_dynasty_prestige = 250 }
		set_culture = culture:german
		hidden_effect = {
			set_character_faith_with_conversion = faith:catholic
			every_vassal = { set_character_faith = faith:catholic }
			every_courtier = { set_character_faith = faith:catholic }
			capital_county = {
				set_county_faith = faith:catholic
			}
		}
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100
	}
}

bzn_novgorod_republic_decision = {
    picture = {
        reference = "gfx/interface/illustrations/decisions/decision_culture.dds"
    }
    decision_group_type = regional_major
    ai_check_interval = 0
    is_shown = {
        OR = {
            culture = { has_cultural_pillar = heritage_north_germanic }
            culture = { has_cultural_pillar = heritage_east_slavic }
        }
		any_held_title = {
			tier = tier_county
			title_province = { geographical_region = custom_novgorod }
		}
		
		OR = {
            has_government = feudal_government
            has_government = tribal_government
            has_government = clan_government
        }
		
        NOT = { government_has_flag = government_is_administrative }
    }
	
    is_valid = {
        has_primary_title = title:k_novgorod
		OR = {
			culture = { has_cultural_pillar = heritage_north_germanic }
			culture = { has_cultural_pillar = heritage_east_slavic }
		}
        is_independent_ruler = yes
    }
	
    desc = novgorod_republic_decision_desc
    cost = { gold = 500 }
    effect = {
		add_prestige = 1000
		if = {
			limit = { has_government = tribal_government }
			convert_whole_realm_to_feudalism_effect = yes
		}
        change_to_administrative_effect = yes
        every_vassal_or_below = {
            limit = {
                OR = {
                    has_government = feudal_government
                    has_government = tribal_government
                    has_government = clan_government
                }
            }
            change_to_administrative_effect = yes
        }
    }
    ai_potential = {
        always = yes
    }
    ai_will_do = {
        base = 0
    }
}

elevate_crete_decision = {
	picture = {
        reference = "gfx/interface/illustrations/decisions/z_ave_maria_sea_travel.dds"
    }
	
	desc = elevate_crete_decision_desc
    decision_group_type = regional_major
    ai_check_interval = 120
	
	cooldown = { years = 1 }

	is_shown = {
		#Standard filter checks.
		is_landed = yes
		has_government = clan_government
		#Must be within the North African cultural sphere.
		culture = { 
			OR = {
				has_cultural_pillar = heritage_berber
				has_cultural_pillar = heritage_arabic
				has_cultural_pillar = heritage_iberian
			}
		}
		#Must be in the vague region.
		any_held_title = {
			tier = tier_county
			title_province = { geographical_region = custom_krete }
		}
		#Can't already be a kingdom.
		highest_held_title_tier <= tier_duchy
		#Not repeatable.
		NOT = {
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_elevated_crete
			}
		}
	}

	is_valid = {
		prestige_level >= 2
		#Must be independent.
		is_independent_ruler = yes
		#Completely controls Krete.
		has_title = title:c_chandax
		has_title = title:c_chania
		has_title = title:d_krete
		#Must be of Muslim Faith
		has_religion = religion:islam_religion
		#Can't already be a kingdom; show this here too so people aren't caught unawares.
		highest_held_title_tier <= tier_duchy
		#Must be in/adjacent to the Viking Age; show this here too so people aren't caught unawares.
	}

	is_valid_showing_failures_only = {
		is_alive = yes
		is_in_an_activity = no
		is_imprisoned = no
	}

	effect = {
		save_scope_as = founder
		# Trigger effects.
		show_as_tooltip = { elevate_crete_scripted_effect = yes }
		trigger_event = { id = eec_major_decisions.0001 }
		#Notify Byzantine Emperor.
		title:e_byzantium.holder = {
    			every_vassal_or_below = {
        			limit = { 
            				is_ai = no
           				 highest_held_title_tier >= tier_county
           				 capital_county = {
               				 is_coastal_county = yes
           			 }
        		}
        		trigger_event = { id = eec_major_decisions.0002 }
    			}
    			if = {
        			limit = { is_ai = no }
   	     			trigger_event = { id = eec_major_decisions.0002 }
    			}
	}
		hidden_effect = {
			add_to_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_elevated_crete
			}
		}
	}

	cost = {
		gold = 250
	}

	ai_potential = {
		always = no
	}

	ai_will_do = {
		base = 0
	}
}

form_oman_decision = {
	picture = { 
		reference = "gfx/interface/illustrations/decisions/decision_realm.dds"
	}
	decision_group_type = regional_major

	ai_check_interval_by_tier = {
		barony = 0
		county = 120
		duchy = 120
		kingdom = 120
		empire = 0
		hegemony = 0
	}

	desc = form_oman_decision_desc
	selection_tooltip = form_oman_decision_tooltip

	is_shown = {
		is_ruler = yes
		is_landed = yes
		exists = dynasty
		title:k_oman = { is_titular = yes } # or the title is titular, because HI can split Arabia and render this obsolete
		capital_county = { duchy = title:d_oman }

		NOT = {	#You can only do it once.
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:form_oman_decision
			}
		}
		NOT = { exists = title:k_oman.holder }
		# Technically Oman is a really Ibadi place but give it some leeway.
		OR = {
			faith.religion = religion:islam_religion
			faith = { has_variable = is_islam_faith }
		}
	}

	is_valid = {
		prestige_level >= 2
		is_independent_ruler = yes
		highest_held_title_tier <= tier_duchy
		#You must control the kingdom's heartlands.
		completely_controls = title:d_oman

	}

	is_valid_showing_failures_only = {
		is_available_adult = yes
	}

	effect = {
		save_scope_as = founder
		found_oman_decision_scripted_effect = yes
		primary_title = { save_scope_as = new_primary_title }
		add_prestige = medium_prestige_gain
		hidden_effect = {
			add_to_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:form_oman_decision
			}
		}
	}

	cost = {
		gold = 250
		piety = 250
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100
	}
}

elevate_catalonia_decision={
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_destiny_goal.dds"
	}
	decision_group_type = regional_major

	ai_check_interval_by_tier = {
		barony = 0
		county = 120
		duchy = 120
		kingdom = 120
		empire = 0
		hegemony = 0
	}

	desc = elevate_catalonia_decision_desc
	selection_tooltip = elevate_catalonia_decision_tooltip

	is_shown = {
		is_ruler = yes
		is_landed = yes
		exists = dynasty # Everyone should have a dynasty, but in case they don't...
		OR = {
			has_title = title:d_barcelona
			any_held_title = {
				tier = tier_county
				duchy = title:d_barcelona
			}
		}
		OR = {
			culture = culture:catalan
			culture = { any_parent_culture_or_above = { this = culture:catalan } }
		}

		NOT = {	#You can only do it once.
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_elevated_catalonia
			}
		}

		# NOT = {
			# has_government = republic_government
		# }

		highest_held_title_tier <= tier_duchy
	}

	is_valid = {
		# is_independent_ruler = yes
		prestige_level >= 2
		completely_controls = title:d_barcelona
	}

	is_valid_showing_failures_only = {
		is_available_adult = yes
	}

	effect = {
		elevate_catalonia_decision_scripted_effect = yes
		primary_title = { save_scope_as = new_primary_title }
		add_prestige = medium_prestige_gain
		hidden_effect = {
			add_to_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_elevated_catalonia
			}
			set_global_variable = {
				name = flag_elevated_catalonia
				value = root
			}
		}
	}

	cost = {
		gold = 250
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100
	}
}

# Majorca
## Either by a king/emperor in Spain or by proclamation.
found_majorca_decision={
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_destiny_goal.dds"
	}
	decision_group_type = regional_major

	ai_check_interval_by_tier = {
		barony = 0
		county = 120
		duchy = 120
		kingdom = 120
		empire = 0
		hegemony = 0
	}

	desc = found_majorca_decision_desc
	selection_tooltip = found_majorca_decision_tooltip

	is_shown = {
		is_ruler = yes
		is_landed = yes
		exists = dynasty # Everyone should have a dynasty, but in case they don't...
		has_title = title:d_mallorca
		highest_held_title_tier <= tier_duchy
		NOT = {	#You can only do it once.
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_found_majorca
			}
		}
	}

	is_valid = {
		# is_independent_ruler = yes
		trigger_if = { # independent
			limit = { highest_held_title_tier <= tier_duchy }
			prestige_level >= 2
		}
		completely_controls = title:d_mallorca
	}

	is_valid_showing_failures_only = {
		is_available_adult = yes
	}

	effect = {
		found_majorca_decision_scripted_effect = yes
		primary_title = { save_scope_as = new_primary_title }
		add_prestige = medium_prestige_gain
		hidden_effect = {
			add_to_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_found_majorca
			}
			set_global_variable = {
				name = flag_found_majorca
				value = root
			}
		}
	}

	cost = {
		gold = 250
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100
	}
}

form_tripolitania_decision = {
	picture = { 
		reference = "gfx/interface/illustrations/decisions/decision_realm.dds"
	}
	decision_group_type = regional_major

	ai_check_interval_by_tier = {
		barony = 0
		county = 120
		duchy = 120
		kingdom = 0
		empire = 0
		hegemony = 0
	}

	title = {
		first_valid = {
			triggered_desc = {
				trigger = { 
					highest_held_title_tier >= tier_empire
				}
				desc = form_tripolitania_decision_overlord
			}
			desc = form_tripolitania_decision
		}
	}

	desc = {
		first_valid = {
			triggered_desc = {
				trigger = { 
					highest_held_title_tier >= tier_empire
				}
				desc = form_tripolitania_decision_desc_overlord
			}
			desc = form_tripolitania_decision_desc
		}
	}
	selection_tooltip = form_tripolitania_decision_tooltip

	is_shown = {
		is_ruler = yes
		is_landed = yes
		exists = dynasty
		OR = {
			capital_province = { geographical_region = custom_tripolitania }
			AND = {
				highest_held_title_tier >= tier_empire
				any_sub_realm_county = { title_province = { geographical_region = custom_tripolitania } }
			}
		}

		NOT = {	#You can only do it once.
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:form_tripolitania_decision
			}
		}
		NOT = { exists = title:k_tripolitania.holder }
	}

	is_valid = {
		prestige_level >= 2
		is_independent_ruler = yes

		OR = {
			highest_held_title_tier >= tier_empire
			highest_held_title_tier <= tier_duchy
		}
		
		completely_controls_region = custom_tripolitania
	}

	is_valid_showing_failures_only = {
		is_available_adult = yes
	}

	effect = {
		found_tripolitania_decision_scripted_effect = yes 
		primary_title = { save_scope_as = new_primary_title }
		add_prestige = medium_prestige_gain
		hidden_effect = {
			add_to_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:form_tripolitania_decision
			}
		}
	}

	cost = {
		gold = 250
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100
	}
}

form_bhutan_decision = {
	picture = {	
		reference = "gfx/interface/illustrations/holding_types/tgp_castle_se_asia.dds"
	}
	decision_group_type = regional_major
	ai_check_interval_by_tier = {
		barony = 0
		county = 120
		duchy = 120
		kingdom = 0
		empire = 0
		hegemony = 0
	}
	desc = form_bhutan_decision_desc
	selection_tooltip = form_bhutan_decision_tooltip

	is_shown = {
		is_ruler = yes
		is_landed = yes
		exists = dynasty
		capital_province = { geographical_region = custom_bhutan }
		highest_held_title_tier < tier_kingdom
		culture = {
			has_cultural_pillar = heritage_tibetan
		}
		OR = {
			faith.religion = religion:buddhism_religion
			faith = { has_doctrine = special_doctrine_is_eastern_faith }
		}
		# Vajrayana and Nangchos, more or less
		faith = { has_doctrine = tenet_esotericism }
		# NOT Theravada
		NOT = { faith = faith:theravada }
		NOT = {
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_formed_bhutan
			}
		}
	}

	is_valid = {
		is_independent_ruler = yes
		prestige_level >= 2
		piety_level >= 2
		OR = {
			has_title = title:d_paro
			has_title = title:d_bumthang
		}
		highest_held_title_tier < tier_kingdom
		completely_controls_region = custom_bhutan
	}

	is_valid_showing_failures_only = {
		is_imprisoned = no
		is_available_adult = yes
	}

	effect = {
		form_bhutan_decision_effect = yes
		primary_title = { save_scope_as = new_primary_title }
		add_prestige = medium_prestige_gain
		hidden_effect = {
			add_to_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_formed_bhutan
			}
		}
	}

	cost = {
		gold = {
			value = 100
		}
		piety = {
			value = 250
		}
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100
	}
}

form_iceland_decision = {
	picture = {	
		reference = "gfx/interface/illustrations/decisions/decision_found_kingdom.dds"
	}
	decision_group_type = regional_major
	ai_check_interval_by_tier = {
		barony = 0
		county = 0
		duchy = 120
		kingdom = 0
		empire = 0
		hegemony = 0
	}
	desc = form_iceland_decision_desc
	selection_tooltip = form_iceland_decision_tooltip

	is_shown = {
		title:k_iceland = { is_titular = yes }
		is_ruler = yes
		is_landed = yes
		is_playable_character = yes
		highest_held_title_tier <= tier_duchy
		exists = dynasty
		capital_county.duchy = title:d_iceland
		culture = {
			has_cultural_pillar = heritage_north_germanic
		}
		NOT = {
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_formed_iceland
			}
		}
	}

	is_valid = {
		is_independent_ruler = yes
		prestige_level >= 2
		completely_controls = title:d_iceland
		realm_size < 8 # If you're big enough, you don't really have any reason to form an Icelandic kingdom.

	}

	is_valid_showing_failures_only = {
		is_imprisoned = no
		is_available_adult = yes
	}

	effect = {
		form_iceland_decision_effect = yes
		primary_title = { save_scope_as = new_primary_title }
		add_prestige = medium_prestige_gain
		hidden_effect = {
			add_to_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_formed_iceland
			}
		}
	}
	cost = {
		gold = 100
		prestige = 250
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100
	}
}

form_albania_decision = {
	picture = { 
		reference = "gfx/interface/illustrations/decisions/decision_realm.dds"
	}
	decision_group_type = regional_major

	ai_check_interval_by_tier = {
		barony = 0
		county = 120
		duchy = 120
		kingdom = 0
		empire = 0
		hegemony = 0
	}

	title = {
		first_valid = {
			triggered_desc = {
				trigger = { is_independent_ruler = yes }
				desc = form_albania_decision
			}
			desc = form_albania_decision_subject
		}
	}

	desc = {
		first_valid = {
			triggered_desc = {
				trigger = { is_independent_ruler = no }
				desc = form_albania_decision_subject_desc
			}
			triggered_desc = {
				trigger = { 
					is_independent_ruler = yes 
					OR = {
						has_primary_title = title:k_sicily
						has_primary_title = title:k_naples
						has_primary_title = title:k_trinacria
					}
				}
				desc = form_albania_decision_desc_italian
			}
			desc = form_albania_decision_desc
		}
	}
	selection_tooltip = form_albania_decision_tooltip

	is_shown = {
		is_ruler = yes
		is_landed = yes
		exists = dynasty
		OR = {
			OR = {
				culture = culture:albanian
				culture = { has_cultural_pillar = language_albanian }
			}
			AND = {
				has_primary_title = title:d_dyrrachion
				is_independent_ruler = no
			}
			has_primary_title = title:k_sicily
			has_primary_title = title:k_naples
			has_primary_title = title:k_trinacria
		}

		NOT = {	#You can only do it once.
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:form_albania_decision
			}
		}
		NOT = { exists = title:k_albania.holder }
		NOT = {
			has_government = tribal_government
			has_government = republic_government
		}
	}

	is_valid = {
		prestige_level >= 2
		trigger_if = {
			limit = { is_independent_ruler = yes }
			is_independent_ruler = yes
		}
		trigger_else = {
			has_strong_usable_hook = top_liege
		}

		highest_held_title_tier <= tier_duchy
		OR = {
			OR = {
				culture = culture:albanian
				culture = { has_cultural_pillar = language_albanian }
			}
			AND = {
				is_independent_ruler = no
				has_primary_title = title:d_dyrrachion
			}
			has_primary_title = title:k_sicily
			has_primary_title = title:k_naples
			has_primary_title = title:k_trinacria
		}
		completely_controls = title:d_dyrrachion
	}

	is_valid_showing_failures_only = {
		is_available_adult = yes
	}

	effect = {
		form_albania_decision_scripted_effect = yes
		hidden_effect = {
			add_to_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:form_albania_decision
			}
		}
	}

	cost = {
		gold = 150
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100
	}
}

create_slovakia_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/decision_destiny_goal.dds" }
	decision_group_type = regional_major

	ai_check_interval = 120

	desc = create_slovakia_decision_desc
	selection_tooltip = create_slovakia_decision_tooltip

	is_shown = {
		is_ruler = yes
		is_landed = yes
		exists = dynasty # Everyone should have a dynasty, but in case they don't...
		OR = {
			culture = culture:hungarian
			culture = culture:slovien
			culture = { any_parent_culture_or_above = { this = culture:slovien } }
		}
		NOT = {	#You can only do it once.
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_created_slovakia
			}
		}
		OR = {	
			has_government = tribal_government
			has_government = feudal_government
		}
		highest_held_title_tier <= tier_duchy
	}

	is_valid = {
		is_independent_ruler = yes
		OR = {
			culture = culture:slovien
			culture = { any_parent_culture_or_above = { this = culture:slovien } }
		}
		prestige_level >= medium_prestige_level
		completely_controls = title:d_nyitra
		completely_controls = title:d_ungvar
	}

	is_valid_showing_failures_only = {
		is_available_adult = yes
	}

	effect = {
		create_slovakia_decision_scripted_effect = yes
		primary_title = { save_scope_as = new_primary_title }
		add_prestige = medium_prestige_gain
		hidden_effect = {
			add_to_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_created_slovakia
			}
		}
	}

	cost = {
		gold = 150
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100
	}
}

create_balaton_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/decision_destiny_goal.dds" }
	decision_group_type = regional_major

	ai_check_interval = 120

	desc = create_balaton_decision_desc
	selection_tooltip = create_balaton_decision_tooltip

	is_shown = {
		is_ruler = yes
		is_landed = yes
		exists = dynasty
		capital_province = { geographical_region = custom_balaton }
		culture = { has_cultural_pillar = heritage_west_slavic }
		OR = {
			culture = culture:slovien
			culture = { any_parent_culture_or_above = { this = culture:slovien } }
		}
		NOT = {	#You can only do it once.
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:form_balaton_decision
			}
		}
		NOT = { exists = title:k_balaton.holder }
	}

	is_valid = {
		prestige_level >= 2
		is_independent_ruler = yes
		highest_held_title_tier <= tier_duchy
		OR = {
			culture = culture:slovien
			culture = { any_parent_culture_or_above = { this = culture:slovien } }
		}
		completely_controls = title:d_somogy
		completely_controls = title:d_gyor
	}

	is_valid_showing_failures_only = {
		is_available_adult = yes
	}

	effect = {
		create_balaton_decision_scripted_effect = yes 
		primary_title = { save_scope_as = new_primary_title }
		add_prestige = medium_prestige_gain
		hidden_effect = {
			add_to_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:form_balaton_decision
			}
		}
	}

	is_valid_showing_failures_only = {
		is_available_adult = yes
	}

	cost = {
		gold = 150
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100
	}
}

form_hyperborea_decision = {
	picture = {	
		reference = "gfx/interface/illustrations/decisions/decision_hyperborea.dds"
	}
	decision_group_type = regional_major
	ai_check_interval_by_tier = {
		barony = 0
		county = 0
		duchy = 0
		kingdom = 36
		empire = 0
		hegemony = 0
	}
	desc = form_hyperborea_decision_desc
	selection_tooltip = form_hyperborea_decision_tooltip

	is_shown = {
		title:e_hyperborea = { is_titular = yes }
		is_ruler = yes
		is_landed = yes
		is_playable_character = yes
		highest_held_title_tier <= tier_kingdom
		exists = dynasty
		OR = {
			has_religion = religion:finno_ugric_religion
			has_religion = religion:siberian_religion
		}
		NOT = {
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_formed_hyperborea
			}
		}
	}

	is_valid = {
		is_independent_ruler = yes
		prestige_level >= 3
		any_held_title = {
			count >= 3
			tier = tier_kingdom
		}
		OR = {
			has_religion = religion:finno_ugric_religion
			has_religion = religion:siberian_religion
			has_religion = religion:shamanism_religion
		}
	}

	is_valid_showing_failures_only = {
		is_imprisoned = no
		is_available_adult = yes
	}

	effect = {
		form_hyperborea_decision_effect = yes
		primary_title = { save_scope_as = new_primary_title }
		add_prestige = 1000
		dynasty = { add_dynasty_prestige = 500 }
		hidden_effect = {
			add_to_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:flag_formed_hyperborea
			}
		}
	}
	cost = {
		gold = 250
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100
	}
}